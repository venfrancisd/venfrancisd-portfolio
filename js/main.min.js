!(function (t) {
    t.fn.isInViewport = function () {
        var e = this[0].getBoundingClientRect();
        return 0 <= e.top && 0 <= e.left && e.bottom <= (window.innerHeight || document.documentElement.clientHeight) && e.right <= (window.innerWidth || document.documentElement.clientWidth)
    }, (function () {
        var e = t(".js-projects-slider"),
            n = t(".js-projects-slider-hint"),
            i = t(".js-projects-slider-hint-icon");
        if (e.length) {
            new Swiper(e.find(".swiper-container"), {
                speed: 600,
                grabCursor: !0,
                slideToClickedSlide: !0,
                preventClicks: !1,
                preventClicksPropagation: !1,
                touchStartPreventDefault: !1,
                spaceBetween: 30,
                navigation: {
                    nextEl: '.swiper-button-next',
                    prevEl: '.swiper-button-prev',
                },
            });
            t(window).on("resize scroll", (function () {
                n.isInViewport() && !n.hasClass("is-hidden") && n.addClass("is-visible")
            })), t(window).scroll(), i[0].addEventListener(function () {
                var e, n = document.createElement("fakeelement"),
                    i = {
                        animation: "animationend",
                        OAnimation: "oAnimationEnd",
                        MozAnimation: "animationend",
                        WebkitAnimation: "webkitAnimationEnd"
                    };
                for (e in i)
                    if (void 0 !== n.style[e]) return i[e]
            }(), (function () {
                e.on("mouseenter mouseleave mousedown mouseup touchstart touchmove touchend", (function () {
                    n.removeClass("is-visible"), n.addClass("is-hidden")
                }))
            }))
        }
    })()
    
    var currentYear = (new Date).getFullYear();
    $(document).ready(function() {
        $("#lyear").text( (new Date).getFullYear() );
    });
})(jQuery);
